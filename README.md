This is practice project. It implements an application that allows crud operations with files.

The application implements operations with following entities:
1. Files:
  - .txt
  - .xml
  - .json
2. Archives:
  - .zip
3. Disks:
  - view disks info
